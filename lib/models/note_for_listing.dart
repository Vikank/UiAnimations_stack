class NoteForListing {
  String? noteId;
  String? noteTitle;
  DateTime? noteCreatedTime;
  DateTime? noteEditedTime;

  NoteForListing(
      {this.noteId, this.noteTitle, this.noteCreatedTime, this.noteEditedTime});
}
